{
  "name": "know-your-neighbors",
  "version": "0.1.0",
  "description": "Know Your Neighbors — an opt-in Slack tool that matches BKSE colleagues by neighborhood and shared interests. Prototype.",
  "private": true,
  "main": "src/matching.js",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "build:demo": "node scripts/build-demo.js",
    "demo": "npm run build:demo && node scripts/serve-demo.js",
    "slack": "node src/slack/app.js",
    "cycle": "node src/slack/run-cycle.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@slack/bolt": "^3.19.0",
    "@slack/web-api": "^6.12.0"
  },
  "license": "UNLICENSED"
}
